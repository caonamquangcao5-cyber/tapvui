# ⚙️ BỘ QUY TẮC DỰ ÁN DÀNH CHO TẤT CẢ AI (PROJECT RULES / AI RULERS)
**Tên file:** `.ai_rules.md` / `PROJECT_RULES.md`  
**Đường dẫn dự án:** `c:\Users\CAO DAI\Desktop\Gym\`  
**Áp dụng:** Tất cả các AI (Antigravity, Cascade, ChatGPT, Claude, Gemini, Cursor...) khi làm việc trong dự án này **BẮT BUỘC PHẢI TUÂN THỦ 100% KHÔNG CÓ NGOẠI LỆ**.

---

## 👤 1. HỒ SƠ NGƯỜI DÙNG & NGUYÊN TẮC GIAO TIẾP (USER PROFILE)

- **Đặc điểm người dùng:** Người vận hành không biết code, không biết tiếng Anh. Chỉ biết đưa ra yêu cầu/vấn đề và sử dụng giao diện phần mềm.
- **Trách nhiệm của AI:** 
  - AI phải tự khảo sát, tự đọc log/chẩn đoán, tự chọn giải pháp kỹ thuật tốt nhất.
  - **TỰ ĐỘNG FIX LỖI & TỐI ƯU:** Khi xảy ra sự cố, AI tự truy vết nguyên nhân gốc và tự sửa code, không hỏi các câu hỏi kỹ thuật phức tạp làm khó người dùng.
  - **Ngôn ngữ:** Trả lời hoàn toàn bằng tiếng Việt ngắn gọn, dễ hiểu, chỉ rõ người dùng cần bấm gì hoặc chạy gì.

---

## 📁 2. CÂY THƯ MỤC CHUẨN & QUY ĐỊNH ĐẶT FILE (DIRECTORY STRUCTURE)

Tất cả các file code mới tạo **PHẢI** nằm đúng vị trí trong cây thư mục sau, tuyệt đối không tạo file tự do ở thư mục gốc (Root):

```text
Gym/
├── .ai_rules.md                  ← Quy tắc này (Ruler cho AI)
├── package.json                  ← Cấu hình dependencies & scripts (root)
├── README.md                     ← Tài liệu hướng dẫn dự án
├── docs/                         ← Thư mục chứa tài liệu thiết kế & hướng dẫn
├── client/                       ← Frontend (React + Vite)
│   ├── public/                   ← File tĩnh (images, icons, favicon)
│   ├── src/
│   │   ├── components/           ← Component dùng chung (UI, layout)
│   │   ├── pages/                ← Trang chính (Login, PTDashboard, StudentView...)
│   │   ├── hooks/                ← Custom React hooks
│   │   ├── services/             ← Gọi API backend
│   │   ├── utils/                ← Helper functions
│   │   ├── context/              ← React Context (auth, theme...)
│   │   ├── App.jsx               ← Root component
│   │   └── main.jsx              ← Entry point frontend
│   └── package.json
├── server/                       ← Backend (Node.js + Express)
│   ├── src/
│   │   ├── routes/               ← Định nghĩa API routes
│   │   ├── controllers/          ← Xử lý logic từng route
│   │   ├── models/               ← Data models (Mongoose / Sequelize...)
│   │   ├── middleware/           ← Middleware (auth, error handling...)
│   │   ├── services/             ← Business logic
│   │   ├── config/               ← Cấu hình (DB, env, constants)
│   │   └── app.js                ← Entry point backend
│   └── package.json
└── .env                          ← Biến môi trường (KHÔNG commit lên git)
```

---

## 🧹 3. QUY ĐỊNH CHỐNG FILE RÁC & TỰ ĐỘNG DỌN DẸP (ANTI-TRASH POLICY)

1. **Tuyệt đối không để lại file rác:**
   - Mọi file test tạm (`test_*.js`, `temp_*`, `scratch_*`, `*.tmp`) tạo ra trong quá trình kiểm thử **PHẢI ĐƯỢC TỰ ĐỘNG XÓA NGAY LẬP TỨC** sau khi test thành công.
   - Không được tạo các file trùng lặp cũ/mới thừa thãi như: `App_old.jsx`, `routes_v1.js`, `test2.js`, `backup.zip`.
   - Không tạo script test/so sánh riêng lẻ ở thư mục gốc (root). Mọi script test phải nằm trong thư mục tạm và tự xóa sau khi xong.
   - Tài liệu thiết kế phải nằm trong thư mục `docs/`, không để ở thư mục gốc.
2. **Không tạo file không cần thiết:**
   - Chỉ tạo file khi thực sự cần thiết cho tính năng đang làm. Không tạo file mẫu, file placeholder, hay file README thừa.

---

## 📏 4. GIỚI HẠN QUY MÔ FILE CODE (< 500 DÒNG)

1. **Giới hạn 500 dòng/file:** Mỗi file mã nguồn (`.js`, `.jsx`, `.ts`, `.tsx`) phải ngắn gọn, mục tiêu **dưới 500 dòng code**.
2. **Quy tắc tách Module:** 
   - Nếu một file code phát triển vượt quá 400 - 500 dòng, AI **BẮT BUỘC** phải chủ động tách các chức năng lớn thành file nhỏ trong cùng thư mục (ví dụ: tách `Booking.jsx` thành `BookingForm.jsx` và `BookingList.jsx`) thay vì dồn tất cả vào 1 file khổng lồ.
   - Việc tách module phải giữ nguyên Public API và không làm đứt gãy kết nối với các module khác.

---

## 🔍 5. QUY TRÌNH DEBUG & KIỂM THỬ THẬT (DEBUG PROTOCOL)

1. **Không sửa lỗi dựa trên suy đoán:** 
   - Khi có lỗi runtime, AI phải đọc log lỗi từ terminal/console hoặc dùng lệnh kiểm tra chính xác nguyên nhân trước khi sửa code.
2. **Không code ảo / không mock giả định:** 
   - Mọi mã nguồn viết ra phải chạy thật 100% trên môi trường thật.
3. **Tự động kiểm tra cú pháp (Syntax Validation):** 
   - Sau khi tạo hoặc chỉnh sửa bất kỳ file `.js`/`.jsx` nào, AI phải tự chạy lệnh kiểm tra (ví dụ `node --check <file.js>` hoặc `npx eslint <file>`) để đảm bảo code không có lỗi cú pháp trước khi bàn giao cho người dùng.

---

## 🎯 6. ĐỊNH HƯỚNG SẢN PHẨM (PRODUCT VISION)

Dự án Gym là một web app vui vẻ dành cho PT và học viên, giúp việc tập luyện trở nên có động lực, gần gũi và nhiều niềm vui hơn.

- **Người dùng chính:**
  - PT nam, sinh năm 1994, quê HCM.
  - Học viên đa số là nữ.
  - Người yêu cầu dự án là học viên nam, 27 tuổi.
- **Mục tiêu:**
  - Tạo một app web truy cập bằng link, không cần cài đặt.
  - PT có thể dùng với nhiều học viên khác nhau.
  - Học viên cảm thấy vui, có động lực và muốn quay lại tập.
  - Tránh cảm giác phần mềm quản lý khô khan.
- **Phong cách:**
  - Vui nhộn, dễ thương, tích cực.
  - Không body-shaming.
  - Không tạo áp lực cân nặng/vóc dáng.
  - Tập trung vào sự cố gắng, cảm xúc, tiến bộ và kết nối với PT.
- **Concept ưu tiên:**
  - `Tập Vui` / `Gym Buddy Quest`
  - Biến mỗi buổi tập thành nhiệm vụ nhỏ, có điểm thưởng, badge, reaction và nhật ký cảm xúc.

---
*Bộ quy tắc này là tiêu chuẩn bắt buộc cho mọi AI tham gia phát triển dự án.*
